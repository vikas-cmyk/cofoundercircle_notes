# src — meeting-api source root

Holds the `meeting_api` Python package (the `pythonpath` for tests, per
`pyproject.toml`). The package is the front door; import from `meeting_api`, never a
deep module path (P6).
