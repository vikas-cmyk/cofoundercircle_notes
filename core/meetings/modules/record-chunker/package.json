{
  "name": "@vexa/record-chunker",
  "version": "0.1.0",
  "description": "Shared browser MediaRecorder driver: wraps a MediaRecorder over a combined audio MediaStream and emits base64 chunks (recording.v1 shape) on each timeslice + a final isFinal chunk on stop. Used by both lane recording taps (gmeet, mixed). Pure browser, zero-dep.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "tsx src/chunker.smoke.test.ts && tsx src/init-segment.smoke.test.ts",
    "check:isolation": "node scripts/check-isolation.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
