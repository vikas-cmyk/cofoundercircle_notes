# golden — flagged-issue.v1 vectors

Conforming examples (the spec, P8). Filename = `<Shape>.<case>.json`; the prefix before the first
dot is the `$def` the vector must conform to (`FlaggedIssue`). Covered: a system auto-flagged
mis-attribution (linked to a captured-signal.v1 frame range), a system-flagged oversegmentation
(linked to a legacy tape), and a human-flagged lost-content issue (with ground truth).
