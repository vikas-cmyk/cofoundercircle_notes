# golden — invocation.v1 vectors

Conforming `Invocation` examples (`Invocation.<case>.json`), validated against `../invocation.schema.json`
by `../validate.mjs`. Secret fields are **placeholders**, never real values (P14).
