# golden — workspace.v1 vectors

Conforming `EntityFrontmatter` examples, validated against `../workspace.schema.json` by `../validate.mjs`.
Extension fields (beyond the base) are allowed — that's the point.
