# agent-api · src

Source root for the `agent-api` service package. The runnable agent control-plane surface is assembled here; see `agent_api/` for the package and the repo-level `core/agent/control_plane` for the shared control-plane logic.
