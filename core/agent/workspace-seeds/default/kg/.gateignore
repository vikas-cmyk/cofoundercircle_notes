# Knowledge-as-code SEED DATA, not a code module. This subtree is the workspace's
# knowledge-graph bundle (OKF v0.1) that agents read/write at runtime — pure content,
# so it opts out of the per-dir README gate (P12). See scripts/gates.mjs walkDirs().
