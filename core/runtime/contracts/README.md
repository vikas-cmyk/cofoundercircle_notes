# contracts — published by `runtime`

Language-neutral contracts this domain owns (JSON Schema + golden vectors). Consumers (other
domains, other languages, SDKs) reference these by path; `gate:schema` validates goldens ≡ schema.
