# transcript-rendering (vendored)

The 0.11 `@vexaai/transcript-rendering` brick (dedup + speaker grouping + two-map
state), vendored verbatim so the sidepanel renders the transcript EXACTLY as the
0.11 dashboard does. esbuild bundles it into sidepanel.js. Promote to a real v0.12
package when the dashboard refactor lands.
