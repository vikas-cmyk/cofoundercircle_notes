# extension/assets/icons — extension icons

The Vexa toolbar and extension-listing icons, referenced by `manifest.json`
(`action.default_icon` + top-level `icons`): `icon16.png`, `icon32.png`,
`icon48.png`, `icon128.png`.
