# vexa_slim

The slim client package — a curated, minimal verb surface over the Vexa API for scripting and cookbook flows. Modules: `client` (HTTP client), `cli`/`__main__` (CLI entry), `config`, `cookbook` (L0–L3 verbs), `harvest`, `models`.
