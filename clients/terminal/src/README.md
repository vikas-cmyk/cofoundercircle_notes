# terminal/src

The Next.js app. `app/` is the thin composition root; the workbench, services, and surfaces live as `@vexa/term-*` bricks under `../modules/` per `../docs/docs/governance/architecture.mdx`.
