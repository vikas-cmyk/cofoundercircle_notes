# contributions

The client-side contribution registry — the `Surface` contract (activity item + slotted views + optional `onSubmit`/commands) and `registerSurface`. The shell reads the registry; surfaces never import each other (P2).
