# api/meeting/jitsi-hosts

`GET` — the deployment's `VEXA_JITSI_HOSTS` list (lowercased hostnames), the env channel the
client-side meeting-link parser (`surfaces/meetingId.ts`) can't read directly. Public, read-only.
