# app tests

App-level unit tests for the terminal client's `src/app/` concern (e.g.
`analytics.test.ts`). Run via the terminal package's test script (`pnpm test`,
Vitest); part of `gate:node` / `pnpm test` in CI.
