{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "test": { "dependsOn": ["build"], "passThroughEnv": ["VEXA_TX_KEY", "VEXA_TX_URL", "EVAL_CACHE"] },
    "lint": {},
    "typecheck": { "dependsOn": ["^build"] }
  }
}
