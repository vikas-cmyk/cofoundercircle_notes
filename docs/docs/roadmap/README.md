# roadmap — the staged delivery plan (Mintlify pages)

`approach.mdx` (how we sequence), `stages.mdx` (the staged plan), `status.mdx` (what's delivered now vs
planned). The current-state half of this should track the MVP0-3 work landed in `clients/terminal` +
`core/agent`.
