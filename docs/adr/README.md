# adr — Architecture Decision Records — one decision per file (context · decision · trade-off)

_Governed by `docs/docs/governance/architecture.mdx` (P1–P22). This folder owns one concern; its public surface is its `index`/contract; it may depend only on what the dependency-rules allow._
