{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vexa.ai/calm/controls/no-egress.requirement.json",
  "title": "Tenant data-egress control",
  "description": "An edge that carries tenant data (e.g. meeting audio) to an external system must declare its egress posture, so a security review can gate it.",
  "type": "object",
  "properties": {
    "dataClass": { "type": "string", "enum": ["audio", "transcript", "pii", "metadata"] },
    "destination": { "type": "string", "enum": ["tenant-hosted", "external-saas"] },
    "egressApproved": { "type": "boolean", "description": "Must be true for any external-saas destination carrying tenant data." }
  },
  "required": ["dataClass", "destination", "egressApproved"]
}
